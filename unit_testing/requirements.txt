# Here we have updated the all the packages without mentioning its version.
# On using pip install -r requirements.txt, Python will automatically install latest and compatible version of all the packages.

# boto3==1.20.26
# pytest==6.2.5
# pytest-cov==3.0.0
# moto==2.3.0

boto3
pytest
pytest-cov
moto
